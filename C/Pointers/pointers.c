#include<stdio.h>

int main() {
    // Prints value in a
    int a=54;
    int b=5;
    printf("%d\n", a);

    //prints the address of a
    printf("%p\n", &a);
    //prints the address of a in integer format
    printf("%d\n", &a);
    // prints the address of a in integer format
    printf("%u\n", &a);

    
    return 0;
}