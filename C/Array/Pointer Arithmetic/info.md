# Pointer Arithmetic
## Basics
pointer difference basically works on the address of variables and not the values stored in the variable itself.
<br>
When a program is run it counts from the first variable and starts allocating addresses next to the last address.