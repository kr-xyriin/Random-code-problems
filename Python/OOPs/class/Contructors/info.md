# Contructors
Invokes at object creation time

## _init_:
All classes have a function called `__init__()` function which is executed when the class is being initiated

### The `self` parameter:
Written as `__init__(self):` ->   
It is the referece to the current object of the class, and is used to access variables that belongs to the class